@model dotNetCarteaDeAur.Models.Shop

@{
    ViewBag.Title = "Shopping page";
    int counter = 0;
}

<h2 class="mb-3">@ViewBag.Title</h2>

<div>

    @foreach (var pub in ViewBag.Publishers)
    {
        <div class="jumbotron text-center">
            <h1>Publisher: @pub.Pub_name</h1>
            <p class="lead mb-5">Our newest collection</p>

            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <div class="row">
                            @foreach (var book in ViewBag.Books)
                            {
                                if (book.Publisher.Pub_name == pub.Pub_name)
                                {

                                    <div class="col-md-3">
                                        <img class="img-fluid" src=@book.ImagePath alt=@book.ImagePath>
                                        <div class="mt-3">
                                            <h5>@book.Title</h5>
                                            <p>@book.Author</p>
                                            @if (Request.IsAuthenticated)
                                            {
                                                <button class="btn"><i class="fa fa-folder"></i> Add to cart</button>
                                            }
                                        </div>
                                    </div>
                                }
                            }
                        </div>
                    </div>
                </div>
                <ol class="carousel-indicators neg-m-3-vw">
                    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                </ol>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>

            </div>
        </div>
    }
</div>

